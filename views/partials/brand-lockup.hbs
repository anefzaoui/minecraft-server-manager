{{! Icon tile + pixel wordmark. Params: size (tile), glyph (icon name),
    glyphSize (icon), textSize (wordmark). Wrap it in the context you need -
    a link in the sidebar, a centered row on the auth pages. }}
<span class="grid {{default size 'size-10'}} shrink-0 place-items-center rounded-md bg-grass-600 text-white shadow-[inset_0_-3px_0_rgb(0_0_0/0.3)]">{{{icon (default glyph 'pickaxe') (default glyphSize 'size-5')}}}</span>
<span class="pixel {{default textSize 'text-sm'}} leading-tight text-ink">Minecraft <span class="text-ok">Server Manager</span></span>
