// Global file manager (/files). All behavior is shared with the per-server
// Files tab: pages/files.js detects the [data-files-global] root and targets
// /api/files instead of /api/servers/:id/files.
import './files.js';
